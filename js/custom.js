chrome.browserAction.onClicked.addListener(function(activeTab){
  //change the host address here
  //localStorage.setItem('refferingURL', window.location.href );
  //localStorage.setItem('mike1','Hello Local Storage' );
  //var newURL = "index.html"+"?jobviewurl="+encodeURIComponent(activeTab.url);
  //chrome.tabs.create({ url: newURL });
});
